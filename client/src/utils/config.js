export const config = {
    development: {
        baseURL: `http://localhost:${process.env.PORT || 5000}`
    },
    production: {
        baseURL: `https://server--blog-system--v5g2jwt46jkk.code.run`
    }
}
